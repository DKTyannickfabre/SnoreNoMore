---
epic: 2
date: 2026-09-01
verdict: accepted
criteria: declared
headless: true
---

# Epic 2 Retrospective — Honest History & Provenance

## Epic summary

Epic 2 ("Honest History & Provenance", covers BA-4, BA-3) delivers two stories:

- 2.1 — Mark simulated nights with an explicit Simulated badge and note
- 2.2 — De-dupe and label multiple same-day nights

Both stories are `done`; `detect-epic --epic 2` returned `pending_stories: []` (story_count 2). No unfinished delivery for this epic.

**Diff range.** Same shared range as Epic 1: `c0b8f6d^..ade08a9` (single build commit + single code-review commit for the whole increment). `git_evidence.py` returns `"stories": []` for both commits — no per-story git attribution is possible; this narrowing was recorded in full in the Epic 1 retro and applies identically here (not re-derived).

**Evidence inventory.**
- Available: `epics.md` declared ACs, both story record files, `CODE-REVIEW-full-cycle.md`, direct read of `snorelab-local/index.html` (`SimulatedBadge`, `dayKey`, `Results` grouping logic), a live behavior check.
- Missing: no automated test suite exists in the project (confirmed pre-existing condition, not epic-specific).

## Findings

| # | Finding | Source | Disposition |
|---|---|---|---|
| F2-1 | `SimulatedBadge` component (L343) renders on every simulated night's row and Detail view; matches story 2.1's AC of an explicit, unambiguous marker (not just a subtle style difference). | `snorelab-local/index.html` L343 vs story 2.1 | Accept as-is |
| F2-2 | `dayKey(ts)` (L843-846) plus `Results`' month/day grouping (L848-~940) groups nights sharing a calendar day under one sub-header while still listing each night's own distinct time range and duration — satisfies both "de-dupe the visual grouping" and "don't collapse genuinely distinct nights" halves of story 2.2's AC. | `snorelab-local/index.html` L843-940 vs story 2.2 | Accept as-is |
| F2-3 | No automated regression coverage exists for the same-day grouping / distinct-night-counting invariant that BA-3 depends on for correctness (this is a project-wide, pre-existing condition, not introduced by this epic). | `CODE-REVIEW-full-cycle.md`; absence of any test tooling in the repo | Fix now (process lesson) → Action item AI-2 |

No findings required a code change to Epic 2's own logic.

## Behavior verification

Exercised live against the served app:

- Opened Results: nights recorded on "Tue, Sep 1" were grouped under one "Tue, Sep 1 · 3 nights" sub-header, with each of the 3 rows showing its own distinct start-end time range (e.g. "5:02 AM – 11:36 AM", "3:54 AM – 11:35 AM", "3:39 AM – 11:37 AM") and its own duration/score — confirming same-day nights are grouped for readability but not merged or miscounted (2.2).
- Every row in the list carried a **Simulated** badge (all test data in this environment is simulated) — confirmed the badge renders consistently across the list view.
- Opened a Detail view for one of the Tue, Sep 1 nights: it showed the **Simulated** badge plus the explanatory note "Demo data — synthesized to show the full flow, not a real measurement." — confirming the badge and note both appear at the Detail level too, not just in the list (2.1).

No discrepancies found between declared ACs, story records, and observed behavior.

## Previous-retro follow-through

No prior retrospective exists for this project. Nothing to follow through on.

## Action items

- **AI-2** (process lesson, owner: dev/QA) — Add regression coverage (even a lightweight manual test script, given the project has no automated test tooling) for the same-day de-dupe/grouping and distinct-night-counting behavior, since it underlies the statistical integrity that Epic 1's Trends aggregation (BA-3) depends on and would be easy to silently regress. Proposed, not applied.

## Acceptance verdict

**Accepted.** Both declared story ACs are demonstrably met: confirmed by direct source-code inspection and independently reconfirmed live (Simulated badge in list and Detail, same-day grouping with distinct per-night time ranges). No unfinished stories (`pending_stories: []`). No blocking findings open — the one action item raised (AI-2) is a process lesson about test coverage, not an unresolved defect or unverified criterion.

## Open questions

- None material to this epic's acceptance.

## Assumptions

- Epic selection: explicit task instruction (run epics 1-4 in order), not auto-detected.
- `detect-epic --epic 2` → `pending_stories: []`, `story_count: 2`, `retro_status: "optional"`. No forced-rejected condition applies.
- Diff range `c0b8f6d^..ade08a9` is shared across all 4 epics (see Epic 1 retro's Assumptions for the full narrowing statement); not re-derived here.
- Headless run: no human available to confirm findings or action items. AI-2 is proposed only; no code was changed and no spec file was edited by this retro.
- Verdict `accepted` is a machine determination based on: declared criteria met, `pending_stories: []`, and no blocking findings — not a human override.
