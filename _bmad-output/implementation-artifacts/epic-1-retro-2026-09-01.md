---
epic: 1
date: 2026-09-01
verdict: accepted
criteria: declared
headless: true
---

# Epic 1 Retrospective — Trustworthy Trends

## Epic summary

Epic 1 ("Trustworthy Trends", covers BA-1, BA-2, UX-1) delivers three stories:

- 1.1 — Gate the Helps verdict behind a minimum nights-per-side threshold (`N_MIN = 2`)
- 1.2 — Show sample-size honesty (n + spread) alongside on/off averages
- 1.3 — Default Trends to a remedy that has both on and off nights

All three stories are `done` in `sprint-status.yaml`; `detect-epic --epic 1` returned `pending_stories: []` (story_count 3), so this epic has no unfinished delivery.

**Diff range.** Every story in this increment (all 4 epics, 9 stories) landed together in a single build commit (`c0b8f6d`), followed by a single code-review commit (`ade08a9`). `git_evidence.py --range c0b8f6d^..ade08a9` reports `"stories": []` for both commits — there is no commit-level attribution to Epic 1 specifically. This is a structural narrowing that applies identically to all four retros in this cycle (recorded once here, see Assumptions).

**Evidence inventory.**
- Available: `_bmad-output/planning-artifacts/epics.md` (declared Given/When/Then ACs), all 3 story record files (each with a self-reported live-verification note), `_bmad-output/implementation-artifacts/CODE-REVIEW-full-cycle.md`, direct read of `snorelab-local/index.html` (helper functions + call sites), a live behavior check against a served copy of the app.
- Missing: no automated test suite exists in this project (static single-file app, no build/test tooling) — all verification is manual/live, confirmed pre-existing in `CODE-REVIEW-full-cycle.md`.

## Findings

| # | Finding | Source | Disposition |
|---|---|---|---|
| F1-1 | `sideStats`, `remedyComparison`, `betterDefaultCandidate`, `findDefaultRemedy` in `index.html` (~L195-245) implement exactly the gating (`N_MIN=2`), n/spread reporting, and both-sides-default logic described in stories 1.1-1.3. No divergence found between spec, story record, and shipped code. | `snorelab-local/index.html` L195-245 vs stories 1.1/1.2/1.3 | Accept as-is |
| F1-2 | Story 1.3 flagged an open tie-break-order question for `betterDefaultCandidate`; code review (`CODE-REVIEW-full-cycle.md`) already resolved this autonomously, keeping the existing order. No new decision needed. | `CODE-REVIEW-full-cycle.md` | Accept as-is (already resolved) |
| F1-3 | `N_MIN = 2` is a hardcoded placeholder threshold; epics.md's own Assumptions section (E4) already flags this as a PM-owned open question, not something this retro discovered. Not re-flagged as a new item. | `epics.md` Assumptions §E4 | Accept as-is (already tracked) |
| F1-4 | All 9 stories across all 4 epics shipped in one commit, and code review ran as a second single commit — no per-story git attribution is possible from history alone. | `git_evidence.py` output | Fix now (process lesson) → Action item AI-1 |

No findings specific to Epic 1's own logic required a code change.

## Behavior verification

Exercised live against a served copy of `snorelab-local/index.html` (`python3 -m http.server 8000`, killed immediately after all four epics' checks completed):

- Opened Trends with existing recorded nights (mix of real/simulated data accumulated across sessions). The default-selected remedy chip was **Side Sleeping** — a remedy with both on- and off-nights present, matching `findDefaultRemedy`'s contract (1.3).
- The on/off comparison displayed sample counts and spread for each side (e.g. "6 nights · ±1.6" / "5 nights · ±1.0"), confirming n+spread are surfaced together, not just the averages (1.2).
- A hedge/caveat line ("Early signal — Side Sleeping averages 2.4 points lower.") appeared, consistent with the high-spread hedging behavior story 1.2 documents.
- Did not independently re-derive the exact numeric threshold gating (`N_MIN=2`) live since it requires constructing a dataset with exactly 1 night on one side; relied on direct source-code confirmation (`sideStats` gating check) plus story 1.1's own recorded live verification for that specific boundary case.

No discrepancies found between declared ACs, story records, and observed behavior.

## Previous-retro follow-through

No prior retrospective exists for this project (`sprint-status.yaml` carries no `epic-*-retrospective` key marked `done` prior to this run, and no earlier retro document is present in `_bmad-output/implementation-artifacts/`). Nothing to follow through on.

## Action items

- **AI-1** (process lesson, owner: dev/build-process) — Future build phases in this autonomous cycle should commit per-story (or at minimum per-epic) rather than landing all stories in one commit. This preserves git-evidence attribution for retrospectives and enables per-story bisection/rollback. Proposed, not applied.

## Acceptance verdict

**Accepted.** All 3 declared story ACs (Given/When/Then in `epics.md`) are demonstrably met: confirmed by direct source-code inspection matching the story records exactly, and independently reconfirmed live (default-remedy selection, n+spread display, hedge copy). No unfinished stories (`pending_stories: []`). No blocking findings open — the one action item raised (AI-1) is a cross-cutting process lesson, not an unresolved defect or unverified criterion.

## Open questions

- None material to this epic's acceptance. (The `N_MIN=2` placeholder value is a pre-existing PM-tracked assumption, not a new open question raised by this retro.)

## Assumptions

- Epic selection: explicit task instruction (run epics 1-4 in order), not auto-detected.
- `detect-epic --epic 1` → `pending_stories: []`, `story_count: 3`, `retro_status: "optional"`. No forced-rejected condition applies.
- Diff range `c0b8f6d^..ade08a9` is shared across all 4 epics because all 9 stories and the subsequent code review landed as two monolithic commits with no per-story commit attribution (`git_evidence.py` returns `"stories": []` for both). This narrowing is recorded once here and applies identically to the Epic 2, 3, and 4 retros in this cycle.
- Headless run: no human available to confirm findings or action items. AI-1 is proposed only; no code was changed and no spec file was edited by this retro.
- Verdict `accepted` is a machine determination based on: declared criteria met, `pending_stories: []`, and no blocking findings — not a human override.
