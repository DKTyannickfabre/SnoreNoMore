---
epic: 4
date: 2026-09-01
verdict: accepted-with-open-items
criteria: declared
headless: true
---

# Epic 4 Retrospective — Confident First Capture

## Epic summary

Epic 4 ("Confident First Capture", covers UX-2) delivers two stories:

- 4.1 — Prime the microphone permission before the OS prompt
- 4.2 — Keep an honest, actionable fallback when permission is denied

Both stories are `done`; `detect-epic --epic 4` returned `pending_stories: []` (story_count 2). No unfinished delivery for this epic.

**Diff range.** Same shared range as Epics 1-3: `c0b8f6d^..ade08a9`, with no per-story git attribution possible (see Epic 1 retro's Assumptions for the full narrowing statement).

**Evidence inventory.**
- Available: `epics.md` declared ACs, both story record files (each verified via mocked `navigator.permissions`/`getUserMedia` since real OS prompts can't be triggered in a test environment), `CODE-REVIEW-full-cycle.md`, direct read of `snorelab-local/index.html` (`startReal`, `MIC_PRIMED_FLAG`, `MicPrimeSheet`, `MicDeniedSheet`).
- Missing: no automated test suite in the project (pre-existing, not epic-specific). Real native permission-prompt behavior cannot be driven by any automated tool in this environment — confirmed directly this session (see Behavior verification).

## Findings

| # | Finding | Source | Disposition |
|---|---|---|---|
| F4-1 | `startReal` (L~1126-1148) queries `navigator.permissions.query({name:'microphone'})` fresh on every start attempt: `denied` → routes straight to `MicDeniedSheet`; not yet `granted` and not previously primed → shows `MicPrimeSheet` once and sets `MIC_PRIMED_FLAG`; otherwise proceeds directly to `beginRealCapture`. This matches AD-2's "no more than once" priming requirement and correctly re-checks state on every call rather than trusting a stale flag, so a later OS-level revocation is still caught and routed to the denied sheet on the next start. | `snorelab-local/index.html` L1126-1148 vs stories 4.1/4.2 | Accept as-is |
| F4-2 | `MicDeniedSheet` (L631-644) offers a Simulate fallback and does not use a blocking `alert()`, matching 4.2's AC for an honest, actionable (not dead-end) denial path. | `snorelab-local/index.html` L631-644 | Accept as-is |
| F4-3 | No automated tool in this environment (nor, per the story records, in the original dev's own verification) can drive a real native microphone-permission prompt end to end; both this retro and the original implementation relied on mocked browser APIs. Confirmed directly this session: triggering "Confirm & Start Night" invoked the real `navigator.permissions.query`/`getUserMedia` path and hung awaiting an actual browser permission decision that no script could resolve. | This session's live attempt; stories 4.1/4.2 | Defer → Action item AI-4 |

## Behavior verification

Exercised live against the served app:

- Read `startReal`'s branching logic directly against the declared ACs and confirmed the priming/denied/granted routing described above (F4-1).
- Attempted to trigger the flow live: clicked "Confirm & Start Night" in the Setup screen. The app called `navigator.permissions.query` and then (since this session's browser storage already had `MIC_PRIMED_FLAG` set from earlier testing, and permission state was not `granted`) proceeded straight into `beginRealCapture`, which called `getUserMedia` and blocked awaiting a real, non-automatable OS/browser permission decision. This is actually positive evidence: it confirms the priming flag correctly suppresses the sheet on a subsequent start (the "no more than once" requirement), and that the gating genuinely reaches the real permission API rather than being a no-op — but it also concretely demonstrates that no further live verification of the prime/deny sheets themselves is possible from this harness. Reloaded the page to clear the stuck state; did not force a fake permission grant/deny since that would test Chromium's mock plumbing rather than the app.
- Relied on stories 4.1 and 4.2's own recorded verification (mocked `navigator.permissions`/`getUserMedia`) for direct confirmation that `MicPrimeSheet` and `MicDeniedSheet` render with the expected copy and controls, cross-checked against the component source read this session (L614-644).

## Previous-retro follow-through

No prior retrospective exists for this project. Nothing to follow through on.

## Action items

- **AI-4** (deferred verification, owner: QA/dev) — Run a manual on-device pass on real iPhone Safari for both paths: priming sheet → Allow → real OS mic prompt → recording starts, and priming/prior-grant → later revoke in OS Settings → next start routes to the denied sheet → Simulate fallback works. Neither this retro nor the original story verification could exercise real native permission prompts; this is a genuine gap for an NFR-sensitive, target-platform-specific flow. Proposed, not applied.

## Acceptance verdict

**Accepted-with-open-items.** Both declared story ACs are met on the evidence available: the priming/denied/granted branching logic matches spec exactly, the denied-fallback avoids a dead-end `alert()`, and this session's live attempt further confirmed the flag-suppression behavior and that the code path genuinely reaches the real permission API. No unfinished stories (`pending_stories: []`), no blocking defect found. Not a plain "accepted" because real native-permission-prompt behavior on the actual target platform (iOS Safari) remains unverified by anyone to date — automated tooling structurally cannot drive it, so it is explicitly deferred and tracked (AI-4) rather than assumed to work.

## Open questions

- Does the priming → Allow → real OS prompt → recording-start sequence work end to end on a real iPhone Safari, and does a later OS-level permission revocation correctly route to the denied sheet on the next start? A human running a manual on-device pass would materially resolve AI-4; this session's evidence (F4-1) suggests the logic is written to handle it correctly, but has not been observed on real hardware.

## Assumptions

- Epic selection: explicit task instruction (run epics 1-4 in order), not auto-detected.
- `detect-epic --epic 4` → `pending_stories: []`, `story_count: 2`, `retro_status: "optional"`. No forced-rejected condition applies.
- Diff range `c0b8f6d^..ade08a9` is shared across all 4 epics (see Epic 1 retro's Assumptions for the full narrowing statement); not re-derived here.
- `MIC_PRIMED_FLAG` was already set to `'1'` in this session's browser `localStorage` prior to this retro's live check (carried over from earlier sessions against the same background tab), which is why the priming sheet did not reappear when the flow was triggered live this session — read as expected behavior of the flag, not a defect, per direct source inspection (F4-1).
- Headless run: no human available to confirm findings or action items. AI-4 is proposed only; no code was changed and no spec file was edited by this retro.
- Verdict `accepted-with-open-items` is a machine determination based on: declared criteria met, `pending_stories: []`, no blocking defect, but one genuinely deferred verification gap (AI-4) — not a human override.
